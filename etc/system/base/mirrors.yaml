mirrors:
  # Source mirror only. OS overlays attach the shared chapar-buildcache binary
  # mirror from CHAPAR_BUILDCACHE_ROOT/<os>, not environment files.
  hpcsim-source:
    binary: false
    url: https://mirror.spack.io
